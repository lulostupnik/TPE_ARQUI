#ifndef STRINGS_H
#define STRINGS_H


// Checks if a character is in a string
// Returns 1 if the character is found, 0 otherwise
int char_in_string(char c, char *str);

#endif // STRINGS_H




/*

// Returns the length of the string
int string_length(char *str);

// Compares two strings
// Returns 0 if they are equal, a positive number if str1 > str2, and a negative number if str1 < str2
int string_compare(char *str1, char *str2);

// Copies the source string into the destination string
void string_copy(char *destination, const char *source);

// Concatenates the source string to the end of the destination string
void string_concatenate(char *destination, const char *source);

 */